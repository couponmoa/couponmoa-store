server:
  port: 8082

spring:
  application:
    name: coupon-server
  profiles:
    active: local

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

grpc:
  server:
    port: 6565